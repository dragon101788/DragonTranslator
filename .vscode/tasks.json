{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "🛠 启动开发",
      "type": "shell",
      "command": "npx",
      "args": ["tauri", "dev"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$rustc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "isBackground": true,
      "detail": "启动 Tauri 开发模式（Vite HMR + Rust），默认 Ctrl+Shift+B"
    },
    {
      "label": "📦 打包便携 EXE",
      "type": "shell",
      "command": "${workspaceFolder}\\打包.bat",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$rustc"],
      "group": "build",
      "detail": "运行 打包.bat（编译 → 复制 exe 到根目录）"
    },
    {
      "label": "📂 打开输出目录",
      "type": "shell",
      "command": "explorer",
      "args": ["${workspaceFolder}\\src-tauri\\target\\release"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "detail": "打开 release 输出目录"
    }
  ]
}
