{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "🛠 启动开发",
      "type": "shell",
      "command": "${workspaceFolder}\\运行.bat",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$rustc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "isBackground": true,
      "detail": "双击 dev.bat 启动开发模式，默认 Ctrl+Shift+B"
    },
    {
      "label": "📦 打包便携 EXE",
      "type": "shell",
      "command": "${workspaceFolder}\\打包.bat",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$rustc"],
      "group": "build",
      "detail": "运行 打包.bat（编译 → 复制 exe 到根目录）"
    },
    {
      "label": "📂 打开输出目录",
      "type": "shell",
      "command": "explorer",
      "args": ["${workspaceFolder}\\src-tauri\\target\\release"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "detail": "打开 release 输出目录"
    }
  ]
}
