{
  "version": "2.0.0",
  "inputs": [
    {
      "id": "tagVersion",
      "type": "promptString",
      "description": "版本号 (如 v0.6.0)",
      "default": "v0.6.0"
    },
  ],
  "tasks": [
    {
      "label": "🛠 启动开发",
      "type": "shell",
      "command": "${workspaceFolder}\\运行.bat",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$rustc"],
      "group": { "kind": "build", "isDefault": true },
      "isBackground": true,
      "detail": "启动 Tauri 开发模式 (Ctrl+Shift+B)"
    },
    {
      "label": "📦 打包发布 ZIP",
      "type": "shell",
      "command": "${workspaceFolder}\\打包.bat",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$rustc"],
      "group": "build",
      "detail": "编译 + 打包 runtime 目录为 DragonTranslator.zip"
    },
    {
      "label": "⬆️ 推送至 GitHub",
      "type": "shell",
      "command": "git push origin master",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": [],
      "detail": "推送本地 commit 到 GitHub"
    },
    {
      "label": "🏷️ 发布 Release",
      "type": "shell",
      "command": "$exe='src-tauri\\target\\release\\龙腾翻译.exe'; $zip='DragonTranslator.zip'; if (-not(Test-Path $zip) -or (Test-Path $exe) -and (Get-Item $exe).LastWriteTime -gt (Get-Item $zip).LastWriteTime) { Write-Output '>>> ZIP 过期或缺失, 重新打包...'; & '.\\打包.bat' 'silent' }; Write-Output '>>> 打 Tag...'; git tag -a ${input:tagVersion} -m '${input:tagVersion}'; git push origin ${input:tagVersion}; Write-Output '>>> Release Tag 已推送'",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": [],
      "detail": "检测 ZIP 是否最新 → 如需要则重新打包 → 打 Tag → push"
    },
    },
    {
      "label": "📂 打开输出目录",
      "type": "shell",
      "command": "explorer",
      "args": ["${workspaceFolder}\\src-tauri\\target\\release"],
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": [],
      "detail": "打开 release 输出目录"
    }
  ]
}
